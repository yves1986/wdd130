/* Reset de base */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* RÈGLES REQUISES PAR LE COURS */

/* 1. Règle nav avec background-color et padding */
nav {
    background-color: #2c3e50;
    padding: 1rem;
    display: flex;
    gap: 2rem;
}

/* 2. Règle nav a avec color, padding, et display: block */
nav a {
    color: white;
    padding: 0.5rem 1rem;
    display: block;
    text-decoration: none;
}

/* 3. Règle main avec max-width */
main {
    max-width: 840px;
    margin: 0 auto;
}

/* 4. Règle h1 avec color (déjà présente) */
h1 {
    color: #2c3e50;
    border-bottom: 3px solid #3498db;
    padding-bottom: 0.5rem;
    margin-bottom: 1rem;
}

/* 5. Règle main img avec toutes les propriétés requises */
main img {
    width: 150px;
    height: auto;
    border: 1px solid #ccc;
    padding: 2px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    float: right;
    margin: 0 0 1rem 1rem;
}

/* 6. Règle aside avec toutes les propriétés requises */
aside {
    width: 300px;
    margin: 1rem;
    border: 1px solid rgba(0, 0, 0, 0.2);
    padding: 1rem;
    background-color: #f8f9fa;
    color: #333;
    text-align: center;
    position: relative;
}

/* 7. Règle aside img avec width: 200px et height: auto */
aside img {
    width: 200px;
    height: auto;
    display: block;
    margin: 0 auto 1rem auto;
}

/* 8. Règle p avec padding: 0 */
p {
    padding: 0;
    margin: 1rem 0;
}

/* 9. Règle footer avec margin-top, border-top, text-align: center */
footer {
    margin-top: 2rem;
    border-top: 1px solid #ccc;
    text-align: center;
    padding: 1rem;
    background-color: #2c3e50;
    color: white;
}

/* Styles supplémentaires pour améliorer l'apparence */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    line-height: 1.6;
}

header {
    background-color: #2c3e50;
    padding: 1rem;
}

.main-content {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin: 2rem 0;
}

.profile-section {
    flex: 1;
    min-width: 300px;
    margin-right: 2rem;
}

.country-info {
    flex: 0 0 300px;
}

/* Nettoyer le float */
.profile-section::after {
    content: "";
    display: table;
    clear: both;
}

/* Responsive design */
@media (max-width: 768px) {
    .main-content {
        flex-direction: column;
    }

    .profile-section {
        margin-right: 0;
        margin-bottom: 2rem;
    }

    main img {
        float: none;
        display: block;
        margin: 1rem auto;
    }

    nav {
        flex-direction: column;
        gap: 0.5rem;
    }

    aside {
        width: 100%;
        margin: 1rem 0;
    }
}